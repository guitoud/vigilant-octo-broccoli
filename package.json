{
  "name": "focom-supports-bp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "fetch-images": "node ./scripts/fetch-images.mjs",
    "build": "npm run fetch-images && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.79.0"
  }
}

---
phase: 02-branding-logo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.astro
autonomous: true
requirements:
  - BRAND-01

must_haves:
  truths:
    - "Le badge texte 'FO' reste visible et inchangé dans le Header"
    - "Header.astro contient un commentaire HTML montrant exactement le code <img> à copier-coller pour remplacer le badge"
    - "Le commentaire documente l'emplacement attendu du fichier logo (public/logo.svg)"
    - "La classe .brand-mark est mentionnée dans le commentaire pour préserver les dimensions du badge"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Slot logo documenté avec commentaire de remplacement prêt à copier"
      contains: "brand-mark"
  key_links:
    - from: "src/components/Header.astro"
      to: "src/styles/global.css"
      via: "classe CSS .brand-mark réutilisée sur le futur <img>"
      pattern: "brand-mark"
---

<objective>
Ajouter un commentaire HTML dans Header.astro documentant exactement le code à copier-coller pour remplacer le badge texte "FO" par un fichier logo image.

Purpose: Permettre à un contributeur de swapper le logo sans chercher comment l'intégrer — le Header lui-même contient les instructions.
Output: Header.astro avec badge texte inchangé + commentaire de slot logo prêt à l'emploi.
</objective>

<execution_context>
@/home/gdumas/code/focom-supports-bp/.claude/get-shit-done/workflows/execute-plan.md
@/home/gdumas/code/focom-supports-bp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Structure actuelle de Header.astro — l'exécuteur travaille sur ce fichier. -->

src/components/Header.astro (ligne 16-24) :
```astro
<header class="site-header">
  <div class="container header-shell">
    <a class="brand" href="/">
      <span class="brand-mark">FO</span>
      <span class="brand-copy">
        <strong>FO Com</strong>
        <span>Supports Banque Postale</span>
      </span>
    </a>
```

src/styles/global.css — classe .brand-mark :
```css
.brand-mark {
  display: inline-grid;
  place-items: center;
  width: 3rem;
  height: 3rem;
  border-radius: 0.9rem;
  background: var(--fo-red);
  color: var(--fo-white);
  font-weight: 700;
  font-size: 1.15rem;
  box-shadow: var(--shadow);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documenter le slot logo dans Header.astro</name>
  <files>src/components/Header.astro</files>

  <read_first>
    - src/components/Header.astro — fichier à modifier, lire l'état actuel avant toute modification
    - src/styles/global.css — vérifier la classe .brand-mark (3rem × 3rem) dont les dimensions sont mentionnées dans le commentaire
  </read_first>

  <action>
Dans src/components/Header.astro, ajouter un commentaire HTML immédiatement avant la ligne `<span class="brand-mark">FO</span>` (ligne 19 actuelle).

Le commentaire doit contenir exactement ce bloc (per D-02, D-03, D-04) :

```html
<!-- Pour remplacer par une image logo, substituer le <span> ci-dessous par :
<img
  src="/logo.svg"
  alt="FO Com Supports Banque Postale"
  width="48"
  height="48"
  class="brand-mark"
/>
Placer le fichier logo dans public/logo.svg (SVG ou PNG acceptés — adapter l'extension src si PNG).
-->
```

Le `<span class="brand-mark">FO</span>` existant reste inchangé après le commentaire (per D-01).

Aucune autre modification : pas de nouveau CSS, pas de logique conditionnelle, pas de nouvel import.
  </action>

  <verify>
    <automated>grep -n "Pour remplacer par une image logo" /home/gdumas/code/focom-supports-bp/src/components/Header.astro && grep -n 'brand-mark>FO</span>' /home/gdumas/code/focom-supports-bp/src/components/Header.astro && npm run build --prefix /home/gdumas/code/focom-supports-bp 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/components/Header.astro contient la chaîne "Pour remplacer par une image logo"
    - src/components/Header.astro contient `src="/logo.svg"`
    - src/components/Header.astro contient `class="brand-mark"` dans le commentaire (sur la balise img)
    - src/components/Header.astro contient `public/logo.svg` (documentation emplacement fichier)
    - src/components/Header.astro contient toujours `<span class="brand-mark">FO</span>` inchangé
    - `npm run build` se termine sans erreur (le commentaire ne casse pas le build Astro)
  </acceptance_criteria>

  <done>Header.astro contient le badge "FO" inchangé et un commentaire HTML prêt à copier-coller montrant le remplacement img avec src="/logo.svg", alt descriptif, dimensions 48×48, classe .brand-mark, et instruction de placement dans public/logo.svg.</done>
</task>

</tasks>

<verification>
- `grep "Pour remplacer par une image logo" src/components/Header.astro` retourne un résultat
- `grep 'brand-mark>FO</span>' src/components/Header.astro` retourne un résultat (badge texte intact)
- `grep 'src="/logo.svg"' src/components/Header.astro` retourne un résultat
- `npm run build` réussit sans erreur
</verification>

<success_criteria>
BRAND-01 satisfait : Header.astro expose un slot image clairement documenté (commentaire HTML avec code prêt à copier) pour remplacer le badge texte "FO" par un logo SVG/PNG, avec l'emplacement du fichier (public/logo.svg) explicitement documenté.
</success_criteria>

<output>
Après exécution, créer `.planning/phases/02-branding-logo/02-01-SUMMARY.md`
</output>

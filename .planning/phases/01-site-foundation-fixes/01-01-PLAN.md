---
phase: 01-site-foundation-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
autonomous: true
requirements:
  - DEPL-02

must_haves:
  truths:
    - "L'URL canonique dans astro.config.mjs est focom-supportsbanquepostale.fr (avec double 'p')"
    - "Les liens canoniques, le sitemap et les OG tags générés lors du build pointent vers le bon domaine"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Configuration Astro avec URL canonique correcte"
      contains: "https://focom-supportsbanquepostale.fr"
  key_links:
    - from: "astro.config.mjs"
      to: "canonical URLs générées"
      via: "champ site:"
      pattern: "site:\\s*\"https://focom-supportsbanquepostale\\.fr\""
---

<objective>
Corriger la typo dans l'URL canonique d'Astro : `focom-suportsbanquepostale.fr` (manque un 'p') → `focom-supportsbanquepostale.fr`.

Purpose: Sans cette correction, chaque build génère des URLs canoniques, des tags OG et un sitemap pointant vers un domaine inexistant. C'est un bug bloquant pour le référencement et le déploiement correct.
Output: `astro.config.mjs` mis à jour avec l'URL correcte.
</objective>

<execution_context>
@/home/gdumas/code/focom-supports-bp/.claude/get-shit-done/workflows/execute-plan.md
@/home/gdumas/code/focom-supports-bp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corriger la typo de l'URL canonique dans astro.config.mjs</name>
  <files>astro.config.mjs</files>
  <read_first>
    - astro.config.mjs — voir le contenu exact actuel avant modification (ligne 4 : champ `site:`)
    - .planning/codebase/CONCERNS.md — section "Known Bugs / Site URL typo" pour confirmation de la valeur attendue
  </read_first>
  <action>
    Dans `astro.config.mjs` ligne 4, remplacer :

    ```
    site: "https://focom-suportsbanquepostale.fr",
    ```

    par :

    ```
    site: "https://focom-supportsbanquepostale.fr",
    ```

    Changement unique : ajouter le 'p' manquant dans "supports" (suports → supports). Ne rien modifier d'autre dans ce fichier.

    Décision D-01 du CONTEXT.md : URL confirmée par le propriétaire du site.
  </action>
  <verify>
    <automated>grep 'site:' /home/gdumas/code/focom-supports-bp/astro.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'site:' astro.config.mjs` retourne exactement : `  site: "https://focom-supportsbanquepostale.fr",`
    - La chaîne `focom-suportsbanquepostale` (sans second 'p') est ABSENTE du fichier : `grep -c 'focom-suportsbanquepostale' astro.config.mjs` retourne `0`
    - Le fichier contient toujours `output: "static"` (aucune autre ligne modifiée)
  </acceptance_criteria>
  <done>astro.config.mjs contient `site: "https://focom-supportsbanquepostale.fr"` et ne contient plus la typo `focom-suportsbanquepostale`</done>
</task>

</tasks>

<verification>
```bash
grep 'site:' /home/gdumas/code/focom-supports-bp/astro.config.mjs
# Attendu :   site: "https://focom-supportsbanquepostale.fr",

grep -c 'focom-suportsbanquepostale' /home/gdumas/code/focom-supports-bp/astro.config.mjs
# Attendu : 0
```
</verification>

<success_criteria>
- astro.config.mjs contient `"https://focom-supportsbanquepostale.fr"` (deux 'p' dans "supports")
- La typo originale `focom-suportsbanquepostale` est absente
- Aucune autre modification au fichier
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-site-foundation-fixes/01-01-SUMMARY.md`
</output>

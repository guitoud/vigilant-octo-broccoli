---
phase: 01-site-foundation-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/contact.ts
  - src/pages/contact/index.astro
autonomous: true
requirements:
  - CONT-01
  - CONT-02

must_haves:
  truths:
    - "La page contact n'affiche qu'une seule entrée Ana-Maria VAZQUEZ (pas de doublons)"
    - "Les données contact (email, représentants, permanences) sont définies dans src/config/contact.ts et non dupliquées dans le template HTML"
    - "La page contact importe et utilise les données depuis src/config/contact.ts"
  artifacts:
    - path: "src/config/contact.ts"
      provides: "Source unique des données contact (email, représentants, permanences)"
      exports: ["contactData"]
      contains: "export const contactData"
    - path: "src/pages/contact/index.astro"
      provides: "Page contact sans doublons, consommant src/config/contact.ts"
      contains: "import { contactData }"
  key_links:
    - from: "src/pages/contact/index.astro"
      to: "src/config/contact.ts"
      via: "import relatif"
      pattern: "import.*contactData.*from.*config/contact"
---

<objective>
Centraliser les données contact dans `src/config/contact.ts` et réécrire `src/pages/contact/index.astro` pour consommer ce fichier, en supprimant les 5 doublons de Ana-Maria VAZQUEZ.

Purpose: La page contact actuelle duplique Ana-Maria VAZQUEZ 5 fois (lignes 31–35) et hardcode toutes les données directement dans le template. La centralisation dans un fichier TypeScript typé permet des mises à jour sans toucher au HTML et satisfait CONT-01 (pas de doublons) et CONT-02 (source unique).
Output: `src/config/contact.ts` (nouveau fichier) + `src/pages/contact/index.astro` (réécrit pour importer les données).
</objective>

<execution_context>
@/home/gdumas/code/focom-supports-bp/.claude/get-shit-done/workflows/execute-plan.md
@/home/gdumas/code/focom-supports-bp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
</context>

<interfaces>
<!-- Contenu actuel de src/pages/contact/index.astro (à remplacer) -->
<!-- Représentants présents dans le fichier actuel : -->
<!--   - Guillaume DUMAS (Coordinateur syndical) — 1 occurrence, à conserver -->
<!--   - Ana-Maria VAZQUEZ (Elu CSE) — 6 occurrences lignes 31–36, réduire à 1 -->
<!--   - Sandrine Birais (Referente QVCT) — 1 occurrence, à conserver -->
<!-- Coordonnées générales : -->
<!--   - Email : contact@focom-supportsbanquepostale.fr -->
<!--   - Permanence : mardi et jeudi de 12h30 à 14h00 -->
<!--   - Site : Immeuble ICV 2 CNP Assurance, Issy Les Moulineaux -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Créer src/config/contact.ts avec les données contact typées</name>
  <files>src/config/contact.ts</files>
  <read_first>
    - src/pages/contact/index.astro — extraire les données exactes à centraliser (email, permanence, site, représentants)
    - .planning/codebase/CONVENTIONS.md — conventions de nommage (camelCase .ts, exports nommés, interfaces PascalCase)
  </read_first>
  <action>
    Créer `src/config/contact.ts` (nouveau fichier — le dossier `src/config/` n'existe pas encore, le créer).

    Le fichier doit exporter une constante `contactData` typée par une interface `ContactData`.

    Structure exacte à créer :

    ```typescript
    export interface Representant {
      role: string;
      nom: string;
    }

    export interface ContactData {
      email: string;
      permanence: string;
      lieu: string;
      representants: Representant[];
      messageRelais: string;
    }

    export const contactData: ContactData = {
      email: "contact@focom-supportsbanquepostale.fr",
      permanence: "mardi et jeudi de 12h30 a 14h00",
      lieu: "Immeuble ICV 2 CNP Assurance, Issy Les Moulineaux",
      representants: [
        { role: "Coordinateur syndical", nom: "Guillaume DUMAS" },
        { role: "Elu CSE", nom: "Ana-Maria VAZQUEZ" },
        { role: "Referente QVCT", nom: "Sandrine Birais" },
      ],
      messageRelais:
        "Les messages peuvent aussi etre relayes vers les representants locaux pour les equipes Supports BP et CNP.",
    };
    ```

    Note : conserver les chaînes sans accents (cohérent avec le fichier source actuel qui utilise "coordonnees", "relayes", etc.).
  </action>
  <verify>
    <automated>node -e "const m = require('/home/gdumas/code/focom-supports-bp/src/config/contact.ts'); console.log('ok')" 2>/dev/null || grep -c 'export const contactData' /home/gdumas/code/focom-supports-bp/src/config/contact.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `src/config/contact.ts` existe
    - `grep -c 'export const contactData' src/config/contact.ts` retourne `1`
    - `grep -c 'export interface ContactData' src/config/contact.ts` retourne `1`
    - `grep -c 'export interface Representant' src/config/contact.ts` retourne `1`
    - `grep -c 'Ana-Maria VAZQUEZ' src/config/contact.ts` retourne `1` (une seule occurrence)
    - `grep 'email:' src/config/contact.ts` contient `contact@focom-supportsbanquepostale.fr`
    - Le tableau `representants` contient exactement 3 entrées : Guillaume DUMAS, Ana-Maria VAZQUEZ, Sandrine Birais
  </acceptance_criteria>
  <done>src/config/contact.ts existe avec une interface ContactData et une constante contactData exportées, contenant 3 représentants sans doublons</done>
</task>

<task type="auto">
  <name>Task 2: Réécrire src/pages/contact/index.astro pour consommer contactData</name>
  <files>src/pages/contact/index.astro</files>
  <read_first>
    - src/pages/contact/index.astro — structure HTML actuelle à conserver (classes CSS, layout, sections)
    - src/config/contact.ts — interface et export créés en Task 1, à importer ici
    - .planning/codebase/CONVENTIONS.md — imports relatifs sans alias (../../config/contact)
  </read_first>
  <action>
    Réécrire `src/pages/contact/index.astro` pour importer `contactData` depuis `../../config/contact` et remplacer toutes les données hardcodées par des références à cet objet.

    Le template doit :
    1. Ajouter `import { contactData } from "../../config/contact";` dans le frontmatter (après les imports existants de BaseLayout, Header, Footer)
    2. Remplacer les valeurs hardcodées dans le HTML par `{contactData.email}`, `{contactData.permanence}`, `{contactData.lieu}`
    3. Remplacer la liste statique des représentants par une boucle `{contactData.representants.map(r => ...)}` — cette boucle rend automatiquement 1 seule entrée Ana-Maria VAZQUEZ
    4. Remplacer le texte du message relais par `{contactData.messageRelais}`

    Conserver exactement : la structure HTML (classes CSS `section`, `container`, `contact-grid`, `contact-card`, `contact-card-alt`, `contact-list`, `eyebrow`), les imports BaseLayout/Header/Footer, le titre de la page, les libellés (`Email :`, `Permanence :`, `Site :`, `Representants`).

    Template cible pour le frontmatter :
    ```
    ---
    import BaseLayout from "../../layouts/BaseLayout.astro";
    import Header from "../../components/Header.astro";
    import Footer from "../../components/Footer.astro";
    import { contactData } from "../../config/contact";
    ---
    ```

    Template cible pour la liste des représentants :
    ```html
    <ul class="contact-list">
      {contactData.representants.map((r) => (
        <li><strong>{r.role} :</strong> {r.nom}</li>
      ))}
    </ul>
    ```
  </action>
  <verify>
    <automated>grep -c 'contactData' /home/gdumas/code/focom-supports-bp/src/pages/contact/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import.*contactData.*from.*config/contact' src/pages/contact/index.astro` retourne `1`
    - `grep -c 'Ana-Maria VAZQUEZ' src/pages/contact/index.astro` retourne `0` (plus de données hardcodées)
    - `grep -c 'Guillaume DUMAS' src/pages/contact/index.astro` retourne `0` (plus de données hardcodées)
    - `grep -c 'contactData\.representants\.map' src/pages/contact/index.astro` retourne `1`
    - `grep -c 'contact@focom' src/pages/contact/index.astro` retourne `0` (email non dupliqué dans le template)
    - `grep -c 'contactData' src/pages/contact/index.astro` retourne au minimum `5` (email, permanence, lieu, map, messageRelais)
    - La classe `contact-grid` est toujours présente : `grep -c 'contact-grid' src/pages/contact/index.astro` retourne `1`
    - La classe `contact-card-alt` est toujours présente : `grep -c 'contact-card-alt' src/pages/contact/index.astro` retourne `1`
  </acceptance_criteria>
  <done>src/pages/contact/index.astro importe contactData, utilise une boucle map pour les représentants (0 données hardcodées de représentants), et toutes les données contact sont consommées depuis src/config/contact.ts</done>
</task>

</tasks>

<verification>
```bash
# Vérifier que src/config/contact.ts existe et exporte correctement
grep 'export const contactData' /home/gdumas/code/focom-supports-bp/src/config/contact.ts

# Vérifier qu'il n'y a qu'une seule Ana-Maria dans le fichier de config
grep -c 'Ana-Maria VAZQUEZ' /home/gdumas/code/focom-supports-bp/src/config/contact.ts
# Attendu : 1

# Vérifier que la page contact n'a plus de données hardcodées de représentants
grep -c 'Ana-Maria VAZQUEZ' /home/gdumas/code/focom-supports-bp/src/pages/contact/index.astro
# Attendu : 0

# Vérifier l'import dans la page contact
grep 'import.*contactData' /home/gdumas/code/focom-supports-bp/src/pages/contact/index.astro
# Attendu : import { contactData } from "../../config/contact";
```
</verification>

<success_criteria>
- `src/config/contact.ts` existe avec interfaces `Representant`, `ContactData` et constante `contactData` exportées
- `contactData.representants` contient exactement 3 entrées (Guillaume DUMAS, Ana-Maria VAZQUEZ x1, Sandrine Birais)
- `src/pages/contact/index.astro` importe `contactData` et n'a plus de données représentants hardcodées
- `grep -c 'Ana-Maria VAZQUEZ' src/pages/contact/index.astro` retourne `0`
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-site-foundation-fixes/01-02-SUMMARY.md`
</output>
